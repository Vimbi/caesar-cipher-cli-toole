const errors = {
  configRequired: 'Config is required',
  configDuplicated: 'Configuration must not be duplicated',
  configNotValid: 'Config is not valid, config must be a string with pattern {XY(-)}n',
  inputDuplicated: 'Input file is not valid, input file must not be duplicated',
  outputDuplicated: 'Output file is not valid, output file must not be duplicated',
}

module.exports = errors;